export interface Recipe {
    id: number,
    title: string,
    description: string,
    difficulty: string,
    imageUrl: string,
    serveSize: string,
    videoUrl: string,
    timeRequired: string,
    categoryName: string,
    userName: string,
    userId: number,
    createdAt: string,
    updatedAt: string
}