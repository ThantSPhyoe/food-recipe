export interface Recipe {
    id: number,
    title: string,
    description: string,
    difficulty: string,
    imageUrl: string,
    userName: string,
    userId: number,
    createdAt: string,
    updatedAt: string
}